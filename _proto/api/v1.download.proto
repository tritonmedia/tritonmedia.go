syntax = "proto3";

package api;

// media definitions
import "v1.media.proto";

message Download {
  // when this job was created
  string createdAt = 1;

  // media is the media we need to download
  Media media = 2;
}